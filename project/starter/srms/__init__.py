"""srms - Student Result Management System.

This file turns the folder into a PACKAGE (Day 16).
It runs once, the first time anything from `srms` is imported.

TODO: re-export the common names so users can write `from srms import Student`
      instead of `from srms.models import Student`.
      Also define __version__ and __all__.
"""

# TODO: from srms.models import ...
# TODO: from srms.exceptions import ...

__version__ = "1.0"
